# nixpacks.toml — Railway build configuration for Candidatic Gateway
# Forces Node.js 20 (required by @whiskeysockets/baileys)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = []

[start]
cmd = "node gateway-server.js"

